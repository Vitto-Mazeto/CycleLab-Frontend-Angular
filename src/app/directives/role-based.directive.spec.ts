import { RoleBasedDirective } from './role-based.directive';

describe('RoleBasedDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleBasedDirective();
    expect(directive).toBeTruthy();
  });
});
